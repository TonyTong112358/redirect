Make an env file with the following syntax
```
FLASK_APP=main.py
FLASK_DEBUG=true


ADMIN_USERNAME=username
ADMIN_PASSWORD=password
```

this now uses 
`pip install -r requirements.txt`

this works, this is ugly

you change the url in /panel

all other paths will redirect 
